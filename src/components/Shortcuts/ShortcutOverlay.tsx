'use client'; import React, { useEffect, useState } from 'react'; import KeyboardHelp from '@/components/floatingChat/KeyboardHelp'; export default function ShortcutOverlay(){ const [open,setOpen]=useState(false); useEffect(()=>{ const onToggle=()=>setOpen(v=>!v); window.addEventListener('neuroedge:toggleHelp', onToggle); return ()=>window.removeEventListener('neuroedge:toggleHelp', onToggle); },[]); return <KeyboardHelp open={open} onClose={()=>setOpen(false)} />; }
