'use client'; import React from 'react'; export default function FloatingChatButton({onClick,open}:any){ return <button aria-label='Open' onClick={onClick} className={'fixed bottom-6 right-6 z-50 w-14 h-14 rounded-full bg-indigo-600 text-white'}>NE</button>; }
