'use client'; import React from 'react'; export default function PermissionModal({open,onClose,onConfirm}:any){ if(!open) return null; return (<div className='fixed inset-0 z-60 flex items-center justify-center bg-black/40'><div className='bg-white p-6 rounded'>Allow scan?<div className='mt-4'><button onClick={onConfirm}>Allow</button><button onClick={onClose}>Cancel</button></div></div></div>); }
