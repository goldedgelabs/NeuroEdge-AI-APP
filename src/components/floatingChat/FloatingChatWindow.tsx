'use client'; import React from 'react'; import { useChatStore } from '@/store/chat'; export default function FloatingChatWindow({open,onClose}:any){ const messages = useChatStore(s=>s.messages); if(!open) return null; return (<div className='fixed bottom-24 right-6 z-50 w-96 bg-white dark:bg-gray-900 border rounded-lg shadow-xl'><div className='p-3 border-b flex justify-between'><div>NeuroEdge Assistant</div><button onClick={onClose}>Close</button></div><div className='p-3 flex-1 overflow-auto'>{messages.map((m:any)=>(<div key={m.id}>{m.text}</div>))}</div></div>); }
