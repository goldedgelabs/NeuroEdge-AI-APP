import { NextRequest } from 'next/server'; import kernel from '@/lib/kernel'; export async function POST(req: NextRequest){ const body = await req.json(); try{ const r = await (kernel.doSend ? kernel.doSend(body) : kernel.send(body)); return new Response(JSON.stringify({ ok:true, r }), { status:200 }); }catch(e){ return new Response(JSON.stringify({ ok:false, error: String(e) }), { status:500 }); } }
